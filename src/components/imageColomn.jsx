import React, {useState} from 'react';

const ImageColomn = () => {
    const [activeTab, setActiveTab] = useState(0);

    return (
        <div>

        </div>
    );
};

export default ImageColomn;